---
import BaseLayout from './BaseLayout.astro';
import Header from '@/components/layout/Header.astro';
import Footer from '@/components/layout/Footer.astro';
import Icon from '@/components/common/Icon.astro';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  updatedDate?: Date;
  heroImage?: string;
  tags?: string[];
}

const { title, description, pubDate, updatedDate, heroImage, tags = [] } = Astro.props;
const canonicalPath = Astro.url.pathname;
const siteUrl = Astro.site ?? new URL('https://jpclow.dev');
const articleUrl = new URL(canonicalPath, siteUrl).toString();

const articleSchema = {
  '@context': 'https://schema.org',
  '@type': 'BlogPosting',
  headline: title,
  description,
  datePublished: pubDate.toISOString(),
  dateModified: (updatedDate ?? pubDate).toISOString(),
  image: heroImage ? [new URL(heroImage, siteUrl).toString()] : undefined,
  mainEntityOfPage: articleUrl,
  author: {
    '@type': 'Person',
    name: 'Joao Paulo Goncalves Santos',
    url: siteUrl.toString(),
  },
  keywords: tags,
};

function formatDate(date: Date): string {
  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
}
---

<BaseLayout
  title={`${title} | João Paulo Santos`}
  description={description}
  ogType="article"
  articlePublishedTime={pubDate.toISOString()}
  articleModifiedTime={(updatedDate ?? pubDate).toISOString()}
  articleTags={tags}
  ogImage={heroImage ?? '/favicon.svg'}
  schema={articleSchema}
>
  <Header />

  <main id="main-content" class="pt-24 pb-16" tabindex="-1">
    <article class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Header -->
      <header class="mb-12">
        <div class="flex flex-wrap gap-2 mb-4">
          {tags.map((tag) => (
            <span class="px-3 py-1 text-xs font-medium bg-[rgb(var(--color-accent)/0.1)] text-[rgb(var(--color-accent))] rounded-full">
              {tag}
            </span>
          ))}
        </div>

        <h1 class="text-4xl sm:text-5xl font-bold mb-4">{title}</h1>

        <p class="text-lg text-[rgb(var(--color-text-secondary))] mb-6">
          {description}
        </p>

        <div class="flex items-center gap-4 text-sm text-[rgb(var(--color-text-muted))]">
          <time datetime={pubDate.toISOString()} class="flex items-center gap-1.5">
            <Icon name="calendar" size={14} />
            {formatDate(pubDate)}
          </time>
          {updatedDate && (
            <>
              <span>•</span>
              <span>Updated: {formatDate(updatedDate)}</span>
            </>
          )}
        </div>
      </header>

      <nav aria-label="Breadcrumb" class="mb-8">
        <ol class="flex items-center gap-2 text-sm text-[rgb(var(--color-text-secondary))]">
          <li>
            <a
              href="/"
              class="rounded-sm hover:text-[rgb(var(--color-accent))] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[rgb(var(--color-accent))] focus-visible:ring-offset-2 focus-visible:ring-offset-[rgb(var(--color-bg-primary))] transition-colors"
            >
              Home
            </a>
          </li>
          <li aria-hidden="true" class="text-[rgb(var(--color-text-muted))]">/</li>
          <li>
            <a
              href="/blog"
              class="rounded-sm hover:text-[rgb(var(--color-accent))] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[rgb(var(--color-accent))] focus-visible:ring-offset-2 focus-visible:ring-offset-[rgb(var(--color-bg-primary))] transition-colors"
            >
              Blog
            </a>
          </li>
          <li aria-hidden="true" class="text-[rgb(var(--color-text-muted))]">/</li>
          <li aria-current="page" class="truncate text-[rgb(var(--color-text-primary))] font-medium">
            {title}
          </li>
        </ol>
      </nav>

      <!-- Hero Image -->
      {heroImage && (
        <div class="mb-12 rounded-2xl overflow-hidden shadow-lg">
          <img
            src={heroImage}
            alt={title}
            class="w-full h-auto"
            loading="lazy"
            decoding="async"
          />
        </div>
      )}

      <!-- Content -->
      <div class="prose prose-lg dark:prose-invert max-w-none">
        <slot />
      </div>

      <!-- Back to blog -->
      <div class="mt-16 pt-8 border-t border-[rgb(var(--color-border))]">
        <a
          href="/blog"
          class="inline-flex items-center gap-2 px-4 py-2 rounded-lg text-[rgb(var(--color-accent))] hover:bg-[rgb(var(--color-accent)/0.1)] transition-colors focus-visible:ring-2 focus-visible:ring-[rgb(var(--color-accent))] focus-visible:ring-offset-2 focus-visible:ring-offset-[rgb(var(--color-bg-primary))] group"
        >
          <span class="group-hover:-translate-x-1 transition-transform">
            <Icon name="arrow-down" size={16} class="rotate-90" />
          </span>
          Back to all posts
        </a>
      </div>
    </article>
  </main>

  <Footer />
</BaseLayout>
